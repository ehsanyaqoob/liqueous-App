import 'package:flutter/material.dart';

class DarkMoodPage extends StatelessWidget {
  const DarkMoodPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
